<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="SnapBoxApp.SettingsPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="Asetukset">

    <ScrollView>
        <VerticalStackLayout Padding="16" Spacing="12">

            <Label Text="API-osoite" />
            <Entry Text="{Binding ApiBaseUrl}" Keyboard="Url" Placeholder="https://api.example.com" />

            <Label Text="Käyttäjätunnus" />
            <Entry Text="{Binding Username}" Placeholder="username" />

            <Label Text="Salasana" />
            <Entry Text="{Binding Password}" IsPassword="True" Placeholder="••••••••" />

            <Button Text="Tallenna" Command="{Binding SaveCommand}" />
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>